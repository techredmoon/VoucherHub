Virtual environment Setup
==========================
install python3
python3 -m venv vh_venv

source vh_venv/bin/activate
deactivate

pip install -r requirements.txt

Create Djangoproject
====================
django-admin startproject voucherhub
cd voucherhub
python manage.py startapp dashboard

Run DjangoProject
=================
python manage.py runserver
http://localhost:8000


GIT COMMANDS
-------------
git clone

git branch
git checkout <your branchname>

git status

#if any update needed
git fetch origin devloper
git pull origin devloper

#devlop code

#check current branch before any push operation
git branch

#push devlop code to your branch
git add .
git commit -m "commit msg"
git push origin <your branchname>

#later after verification we merge it to devloper branch
